<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/css/bootstrap.min.css" rel="stylesheet">
  <link rel="stylesheet" href="/style.css">
  <title>Edit Product</title>
</head>
<body>
  <%- include('partials/navbar', { user: user }) %>

  <section class="sa-hero">
    <div class="container">
      <div class="eyebrow">Crew tools</div>
      <h1>Update product details.</h1>
      <p>Keep information up to date with a premium, SIA-inspired admin screen.</p>
    </div>
  </section>

  <main class="container page-shell">
    <div class="row justify-content-center">
      <div class="col-12 col-md-8 col-lg-6">
        <% if (product) { %>
          <div class="card">
            <div class="card-body">
              <h2 class="text-center mb-4">Update Product</h2>
              <form action="/editProduct/<%= product.id %>" method="POST" enctype="multipart/form-data">
                  <div class="mb-3">
                    <label for="name" class="form-label">Product Name</label>
                    <input type="text" id="name" name="name" class="form-control" value="<%= product.productName %>" required>
                  </div>

                  <div class="row g-2">
                    <div class="col-6">
                      <label for="quantity" class="form-label">Quantity</label>
                      <input type="number" id="quantity" name="quantity" min="0" step="1" value="<%= product.quantity %>" class="form-control" required>
                    </div>
                    <div class="col-6">
                      <label for="price" class="form-label">Price</label>
                      <input type="number" id="price" name="price" min="0" step="0.01" value="<%= product.price %>" class="form-control" required>
                    </div>
                  </div>

                  <div class="mb-3 mt-3">
                    <label for="currentImage" class="form-label">Current Image</label>
                    <input type="text" name="currentImage" id="currentImage" class="form-control" value="<%= product.image %>" readonly>
                    <img src="/images/<%= product.image %>" class="img-thumbnail mt-2" style="width: 150px;">
                  </div>
                  
                  <div class="mb-3">
                    <label class="form-label" for="image">New Image (optional)</label>
                    <input type="file" id="image" name="image" accept="image/*" class="form-control">
                  </div>

                  <div class="d-grid gap-2">
                    <button type="submit" class="btn btn-primary">Update Product</button>
                    <a href="/shopping" class="btn btn-outline-primary">Back to shop</a>
                  </div>
              </form>
            </div>
          </div>
        <% } else { %>
          <div class="alert alert-info text-center">No product found.</div>
        <% } %>
      </div>
    </div>
  </main>
</body>
</html>
