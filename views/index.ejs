<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/css/bootstrap.min.css" rel="stylesheet">
  <link rel="stylesheet" href="/style.css">
  <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/js/bootstrap.bundle.min.js"></script>
  <title>Supermarket App</title>
</head>
<body>
    <%- include('partials/navbar', { user: user, simple: true }) %>

    <section class="sa-hero">
      <div class="container">
        <div class="eyebrow">Seasonal inspiration</div>
        <h1>Welcome to your neighborhood supermarket.</h1>
        <p>Fresh produce, pantry staples, and everyday essentials delivered with care. Stock up on everything you need for the week ahead.</p>
        <div class="hero-actions">
          <a class="btn btn-primary" href="/shopping">Start shopping</a>
          <% if (!user) { %>
            <a class="btn btn-outline-primary" href="/register">Create an account</a>
          <% } else { %>
            <a class="btn btn-outline-primary" href="/cart">View cart</a>
          <% } %>
        </div>
      </div>
    </section>

    <main class="container page-shell">
      <div class="hero-card mb-4">
        <div>
          <div class="pill-tag mb-1">Your account</div>
          <% if (user) { %>
            <h4 class="mb-1 text-white">Hi <%= user.username %>, ready to stock up?</h4>
            <p class="mb-0 muted-text">Jump back into your pantry list below.</p>
          <% } else { %>
            <h4 class="mb-1 text-white">Unlock tailored deals.</h4>
            <p class="mb-0 muted-text">Log in or sign up to enjoy member-only savings and checkout shortcuts.</p>
          <% } %>
        </div>
        <div class="d-flex gap-2">
          <% if (!user) { %>
            <a class="btn btn-primary" href="/login">Log in</a>
            <a class="btn btn-outline-primary" href="/register">Sign up</a>
          <% } else if (user.role === "admin") { %>
            <a class="btn btn-primary" href="/addProduct">Add product</a>
            <a class="btn btn-outline-primary" href="/admin/analytics">Analytics</a>
          <% } else { %>
            <a class="btn btn-primary" href="/shopping">Continue shopping</a>
            <a class="btn btn-outline-primary" href="/history">Purchase history</a>
          <% } %>
        </div>
      </div>

      <div class="row g-3">
        <div class="col-md-6">
          <div class="card">
            <div class="card-body">
              <h5 class="card-title">Browse essentials</h5>
              <p class="card-text text-muted">Search by category, add to cart quickly, and keep your kitchen stocked.</p>
              <a class="btn btn-primary" href="/shopping">Shop now</a>
            </div>
          </div>
        </div>
        <div class="col-md-6">
          <div class="card">
            <div class="card-body">
              <h5 class="card-title">Stay on top of orders</h5>
              <p class="card-text text-muted">Check your cart, track your history, and revisit favourites anytime.</p>
              <div class="d-flex gap-2">
                <a class="btn btn-outline-primary" href="/cart">Cart</a>
                <a class="btn btn-outline-primary" href="/history">History</a>
              </div>
            </div>
          </div>
        </div>
      </div>
    </main>
</body>
</html>
