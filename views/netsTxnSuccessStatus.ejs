<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8" />
  <meta name="viewport" content="width=device-width, initial-scale=1.0" />
  <title>Payment Successful</title>
  <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/css/bootstrap.min.css" rel="stylesheet">
  <link rel="stylesheet" href="/style.css">
</head>
<body>
  <%- include('partials/navbar', { user: user }) %>

  <section class="sa-hero">
    <div class="container">
      <div class="eyebrow">Payment complete</div>
      <h1><%= message %></h1>
      <p>Your order has been confirmed. Thank you for shopping with us.</p>
    </div>
  </section>

  <main class="container page-shell">
    <div class="panel light p-4 text-center">
      <img height="auto" width="120" src="/images/greenTick.png" alt="Transaction Success" />
      <% if (orderId) { %>
        <p class="mt-3 mb-0">Order #<%= orderId %></p>
      <% } %>
      <div class="mt-4">
        <a class="btn btn-primary" href="/history">View orders</a>
        <a class="btn btn-outline-primary ms-2" href="/shopping">Keep shopping</a>
      </div>
    </div>
  </main>
</body>
</html>
