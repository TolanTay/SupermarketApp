<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="utf-8" />
  <meta name="viewport" content="width=device-width,initial-scale=1" />
  <title>Register</title>
  <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/css/bootstrap.min.css" rel="stylesheet" />
  <link rel="stylesheet" href="/style.css">
</head>
<body>
  <%- include('partials/navbar', { user: user, simple: true }) %>

  <section class="sa-hero">
    <div class="container">
      <div class="eyebrow">Fresh groceries, delivered with care</div>
      <h1>Join to save your cart and speed up checkout.</h1>
      <p>Create an account to keep favourites, track orders, and make every grocery run smoother.</p>
      <div class="hero-actions">
        <a class="btn btn-outline-primary" href="/">Return home</a>
      </div>
    </div>
  </section>

  <main class="container page-shell" style="max-width:760px;">
    <h2 class="mb-4">Create an account</h2>

    <% if (messages && messages.error && messages.error.length) { %>
      <div class="alert alert-danger">
        <% messages.error.forEach(function(err){ %>
          <div><%= err %></div>
        <% }) %>
      </div>
    <% } %>

    <% if (messages && messages.success && messages.success.length) { %>
      <div class="alert alert-success">
        <% messages.success.forEach(function(msg){ %>
          <div><%= msg %></div>
        <% }) %>
      </div>
    <% } %>

    <div class="card shadow-sm">
      <div class="card-body">
        <form action="/register" method="POST" class="needs-validation" novalidate>
          <div class="mb-3">
            <label for="username" class="form-label">Username</label>
            <input id="username" name="username" type="text" class="form-control" required
              value="<%= formData && formData.username ? formData.username : '' %>">
          </div>

          <div class="mb-3">
            <label for="email" class="form-label">Email</label>
            <input id="email" name="email" type="email" class="form-control" required
              value="<%= formData && formData.email ? formData.email : '' %>">
          </div>

          <div class="mb-3">
            <label for="password" class="form-label">Password</label>
            <input id="password" name="password" type="password" class="form-control" required>
          </div>

          <div class="mb-3">
            <label for="address" class="form-label">Address (optional)</label>
            <input id="address" name="address" type="text" class="form-control"
              value="<%= formData && formData.address ? formData.address : '' %>">
          </div>

          <div class="mb-3">
            <label for="contact" class="form-label">Contact number (optional)</label>
            <input id="contact" name="contact" type="text" class="form-control"
              value="<%= formData && formData.contact ? formData.contact : '' %>">
          </div>

          <div class="d-grid">
            <button class="btn btn-primary">Register</button>
          </div>
        </form>
      </div>
    </div>

    <p class="mt-3">Already have an account? <a href="/login">Login</a></p>
  </main>
</body>
</html>
